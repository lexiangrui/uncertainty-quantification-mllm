# 论文解读：SemGrad - 用语义保持表征的梯度衡量 LLM 不确定性

> **论文**：Gradients with Respect to Semantics Preserving Embeddings Tell the Uncertainty of Large Language Models  
> **作者**：Mingda Li, Rundong Lv, Xinyu Li, Weinan Zhang, Ting Liu  
> **版本**：arXiv:2605.04638v2，2026-06-01；ICML 2026  
> **链接**：[arXiv](https://arxiv.org/abs/2605.04638) · [PDF](https://arxiv.org/pdf/2605.04638) · [代码](https://github.com/mingdali6717/SemGrad)  
> **关键词**：Uncertainty Quantification、Free-form Generation、Semantic Gradient、White-box UQ、Sampling-free

## 1. 一句话总结

论文提出 **SemGrad**：如果模型真的确信某个回答，那么输入语义发生很小变化时，回答概率应该保持稳定；因此，可以用“生成回答的加权对数似然对模型内部语义表征的梯度大小”作为不确定性分数，梯度越大，模型越不确定。

需要特别澄清：SemGrad **并不在每次推理时真的生成释义或显式扰动输入**。它在一个语义保持的隐藏表示点上计算局部导数，用梯度近似无穷小语义扰动下输出的敏感度，所以推理阶段不需要多次采样。

## 2. 论文要解决什么问题？

自由文本生成的输出空间极大，同一个问题还可能有多个含义相同但措辞不同的正确回答。现有较强的 UQ 方法（如 Semantic Entropy、SAR）通常需要：

1. 对同一问题采样多个回答；
2. 判断回答之间是否语义等价；
3. 根据语义簇、相似度或密度估计不确定性。

这种路线计算开销大，估计结果也受到采样方差影响。传统梯度 UQ 虽然免采样，但大多来自分类任务：若每个输入只有一个真实标签，训练最优点附近该标签的参数梯度应接近零，于是参数梯度大可解释为模型不确定。

作者指出，这个推理在自由生成中会失效。自然语言的真实条件分布通常是多峰的，即存在不可约的 **偶然不确定性（aleatoric uncertainty）**。即使模型已经学得很好，某一个合法回答的参数梯度也不一定为零。因此，直接把参数梯度大等价为“知识不足”可能产生误判。

## 3. 核心直觉

作者的假设是：

> 语义等价的输入，应当诱导相近的真实输出分布；一个对当前问题有把握的模型，也应对微小的语义保持扰动保持稳定。

设模型针对输入 $x$ 生成回答 $\hat y$，$h_E(x)$ 是能够保持输入语义的内部表征。若模型确信该回答，则应有：

$$
\left\|\nabla_{h_E}\log p(\hat y\mid x,\theta;h_E(x))\right\|\approx 0.
$$

反过来，若这个梯度很大，说明沿内部“语义空间”稍微移动，就可能显著改变当前回答的概率，模型在该点附近不稳定，因而具有较高不确定性。

这个量更准确的名称是 **局部语义敏感度**。它是一个排序分数，并不天然等于“回答错误的概率”，也不能直接视为经过校准的置信度。

## 4. 方法拆解

### 4.1 SPS：先找到真正承载语义的隐藏状态

不是任意隐藏状态都能代表完整语义。论文定义 **Semantic Preservation Score（SPS）** 来选择层和 token 位置。

对每个问题 $x_n$ 生成 $K$ 个语义等价的释义，加上原问题共 $K+1$ 个版本。对于第 $l$ 层、倒数第 $t$ 个输入 token 的隐藏状态 $h^{(l)}_{-t}$，计算：

- 同一问题不同释义之间的平均余弦相似度 $S^{l,t}_{w/i}$；
- 不同问题之间的平均余弦相似度 $S^{l,t}_{a/c}$；
- 二者之差：

$$
\operatorname{SPS}(h^{(l)}_{-t})=S^{l,t}_{w/i}-S^{l,t}_{a/c}.
$$

SPS 高意味着该表示同时满足“同义输入靠近、不同语义分开”。实验发现：

- 每个模型都有一个相对稳定的 **Semantic Preserving Token**，记作 $t^*$；
- $t^*$ 与模型的 chat template 有关，而不一定是用户问题的最后一个普通 token；
- 较深的半数层语义保持能力更强；
- 最后一层主要服务于下一个 token 解码，作者没有把它纳入语义梯度。

最终使用 $t^*$ 位置上从中间层到倒数第二层的隐藏状态拼接：

$$
h^{\uparrow}_{t^*}=\operatorname{Concat}\left(h^{(L/2+1)}_{t^*},\ldots,h^{(L-1)}_{t^*}\right).
$$

论文的具体选择是：Llama 3.1 Instruct 8B 的 `<|start_header_id|>`、Qwen3 Instruct 4B 的 `<|im_start|>`，以及 Mistral Nemo Instruct 12B 的最后一个用户输入 token。

### 4.2 SemGrad：对语义表征求梯度

生成回答为 $\hat y=(\hat y_1,\ldots,\hat y_T)$。作者没有让所有输出 token 等权，因为停用词、子词等通常语义贡献较小。第 $t$ 步使用词表分布熵作为权重：

$$
\omega_t=H\bigl(p(y_t\mid \hat y_{<t},x)\bigr).
$$

最终分数为：

$$
S_{\text{SemGrad}}
=\frac{1}{|h^{\uparrow}_{t^*}|}
\left\|
\nabla_{h^{\uparrow}_{t^*}}
\sum_{t=1}^{T}\omega_t
\log p(\hat y_t\mid \hat y_{<t},x,\theta;h^{\uparrow}_{t^*})
\right\|_1.
$$

其中 $\omega_t$ 在反向传播时被 detach，只作为固定权重，不让其自身的梯度进入结果。最终采用按维度归一化的 $L_1$ 范数。分数越大表示越不确定。

注意，论文把“高熵 token 更有信息”当作轻量代理。它能压低大量低熵冗余 token 的影响，但高熵不必然等价于语义重要，专名的确定性 token 或格式性却高熵的 token 都可能构成反例。

### 4.3 ParaGrad：参数空间的补充信号

在单一正确答案、低偶然不确定性的任务中，参数梯度与训练目标直接对齐，数值上可能比 SemGrad 更稳定。作者构造了同样带 token 熵权重的参数梯度：

$$
S_{\text{ParaGrad}}
=\frac{1}{|\theta|}
\left\|
\nabla_{\theta}
\sum_{t=1}^{T}\omega_t
\log p(\hat y_t\mid \hat y_{<t},x,\theta)
\right\|_1.
$$

公式写作对全部参数 $\theta$ 求导，但实验实现为了效率只对 LM head 权重求导。

### 4.4 HybridGrad：依据平均 token 熵动态融合

令平均 token 熵为：

$$
\bar\omega=\frac{1}{T}\sum_{t=1}^{T}\omega_t,
$$

则：

$$
S_{\text{HybridGrad}}
=(1-e^{-\bar\omega})S_{\text{SemGrad}}
+e^{-\bar\omega}S_{\text{ParaGrad}}.
$$

- 低熵时，更相信参数梯度；
- 高熵时，更相信语义梯度。

这个设计直观，但平均预测熵只是任务偶然不确定性的代理，也会混入模型无知、校准误差和解码上下文效应。附录加入缩放系数 $\tau$ 与 $\beta$ 后，融合结果会随超参数变化，说明两种梯度的量纲和权重仍有进一步校准空间。

## 5. 实验设计

### 数据集与模型

- **SciQ**：1,000 个验证问题，单一语义正确答案；
- **TriviaQA**：开放域测试集 11,313 个问题，单一语义正确答案；
- **TruthfulQA**：817 个问题，许多题目允许多个合理答案，偶然不确定性较高；
- 模型：Qwen3-Instruct-4B、Mistral-Nemo-Instruct-12B、Llama3.1-Instruct-8B；
- 模型以 greedy decoding 生成短回答；
- 主要用 BEM 判断回答正确与否，并以 UQ 分数区分正确/错误回答的 **AUROC** 为主指标；附录报告 AURC，并在 Llama 3.1 上用 DeepSeek 作为 judge 做稳健性检查。

对比方法共 11 种，包括 LN-PE、P(True)、Self-Consistency、Deg、INSIDE、Semantic Entropy、Semantic Density、Mutual Information、G-NLL、SAR 和 ExGrad。

### 主要结果

| 方法 | 平均 AUROC | Qwen / TruthfulQA | Mistral / TruthfulQA | Llama / TruthfulQA |
|---|---:|---:|---:|---:|
| SAR | 75.33 | 67.98 | 68.55 | 64.44 |
| ExGrad | 73.92 | 63.77 | 66.40 | 62.00 |
| ParaGrad | 75.59 | 66.40 | 70.54 | 63.91 |
| **SemGrad** | **75.75** | **69.06** | **72.27** | **69.42** |
| **HybridGrad** | **76.59** | **69.61** | **72.72** | 69.25 |

结论可以概括为：

1. **HybridGrad 的总体平均 AUROC 最高（76.59）**；
2. 在高偶然不确定性的 TruthfulQA 上，SemGrad 明显比参数梯度方法稳定；
3. SemGrad 在三个模型的 TruthfulQA 上平均约为 70.25，SAR 约为 66.99，提升约 **3.27 个百分点**；
4. 单答案数据集上 ParaGrad 有时优于 SemGrad，例如 Qwen/TriviaQA、Mistral/SciQ 和 Mistral/TriviaQA；
5. AURC 的总体趋势一致，HybridGrad 平均 AURC 最低（28.65），SemGrad 次之（28.97）。

论文正文声称 TruthfulQA 上 SemGrad 比 ExGrad 平均高 **6.82** 点，但按表 1 的三个模型数值重新计算，SemGrad 为 70.25、ExGrad 为 64.06，差值约 **6.19** 点。这里可能是正文或表格版本未同步，引用该数字时应谨慎。

### 消融结果

- $L_1$ 与 $L_2$ 梯度范数差异很小；
- 去掉 token 熵权重后，六个模型-数据集组合全部下降，TriviaQA 上降幅尤其明显；
- 在 SPS 高的层/token 上计算 SemGrad，AUROC 也更高；
- 使用语义 token $t^*$ 通常优于简单使用输入最后一个 token；
- 使用较深的半数层不是每个单项最优，但跨数据集更稳定。

这些消融支持“找到语义保持表征很重要”，但它们仍主要是相关性证据：高 SPS 与高 UQ AUROC 同时出现，并没有严格证明梯度方向真的对应自然、可实现的语义保持扰动。

## 6. 效率与工程代价

在单张 A100 80GB、Llama 3.1 Instruct 8B、batch size 1 下，每个样本相对纯生成的额外耗时如下：

| 方法 | SciQ | TriviaQA | TruthfulQA |
|---|---:|---:|---:|
| SemGrad | +0.2506 s | +0.2577 s | +0.1979 s |
| HybridGrad | +0.2780 s | +0.2878 s | +0.2287 s |
| SAR | +0.3632 s | +0.4715 s | +0.5542 s |
| Semantic Entropy | +0.3754 s | +0.5093 s | +0.5790 s |
| Semantic Density | +1.6502 s | +1.7173 s | +1.8917 s |

所以“sampling-free”并不等于零成本：SemGrad 仍需一次反向传播和保存激活，接近一次训练 step。论文给出的内存复杂度为 $O(LTD)$；采样 $K$ 次的方法约为 $K\cdot O(LTD)$，且可能还需要相似度模型或语义聚类。

当前 Transformers 实现还不能只对选定 token 的子张量高效求梯度，作者实际对输入序列全部隐藏状态求导，再取所需位置，产生了额外开销。理论上仍有工程优化空间。

## 7. 我认为这篇论文最有价值的地方

1. **把梯度 UQ 从参数空间转向语义表示空间。** 这比把分类任务的参数梯度直接套到自由生成上更符合自然语言多答案的结构。
2. **无需在每个测试样本上生成 5-10 个候选答案。** 它避免了采样方差，也省去在线语义聚类或辅助相似度模型。
3. **SPS 给出了可操作的表示选择准则。** 论文没有武断地选“最后一层最后一个 token”，而是实证寻找同义不变、异义可分的层/token。
4. **对高偶然不确定性场景的分析较清楚。** TruthfulQA 上语义梯度与参数梯度的分化，是全文最有说服力的现象。
5. **暴露了一个值得继续研究的视角。** UQ 不一定只来自输出概率或多次采样，也可以来自模型内部表示到输出的局部几何。

## 8. 局限与质疑

### 8.1 它衡量的是局部敏感度，不是完整的不确定性

梯度只描述一阶、无穷小邻域。模型可能在当前点梯度很小，但稍远处迅速变化；也可能因饱和、归一化或表示尺度导致梯度小，而非真的有把握。更完整的局部几何可能需要 Hessian、有限差分或沿语义流形的扰动实验。

### 8.2 “隐藏空间的小扰动”不保证语义保持

SPS 说明某个隐藏状态能区分释义与不同问题，却不能保证该空间任意小方向都沿自然语言的语义流形。当前 SemGrad 计算的是完整梯度范数，其中可能包含脱离数据流形、不可由真实输入实现的方向。更严格的做法应把梯度投影到由释义差分、语义子空间或生成式扰动器估计出的切空间。

### 8.3 SPS 的准备阶段并非完全“免外部数据/模型”

推理阶段确实免采样，但选择 $t^*$ 和层范围时，作者为每个问题用 DeepSeek API 生成 5 个释义，再用 NLI/LLM judge 验证释义质量。因此，方法的离线配置仍依赖额外生成器、代表性语料和模型/chat template。更换基础模型、模板或领域后，可能需要重做 SPS 扫描。

### 8.4 只适用于白盒模型

必须访问内部隐藏状态、计算图和权重梯度，无法直接用于 GPT、Claude 等闭源 API。反向传播也会增加显存与延迟，实际部署约束不同于仅做多次前向采样。

### 8.5 实验范围仍窄

- 只测试 3 个 4B-12B 开源文本模型；
- 主要是短回答 QA，作者也承认长文本中梯度会被大量 token 稀释；
- 没有数学推理、代码生成、对话、长上下文或分布外数据；
- 主指标是回答正确性分类 AUROC，而不是 ECE、Brier score、NLL 等概率校准指标；
- 主要正确性标签来自自动指标 BEM，LLM judge 检查只覆盖一个模型，没有大规模人工标注。

因此，目前证据足以说明它是一个有竞争力的 **错误检测排序分数**，但还不能说明它能输出可靠、可解释、跨任务校准的概率。

### 8.6 HybridGrad 的融合还有经验性

平均 token 熵不等于偶然不确定性，SemGrad 与 ParaGrad 的数值尺度也未必天然可比。附录中引入 $\tau$ 和 $\beta$ 会改变结果，说明固定的指数权重并非唯一或已被充分证明的选择。

## 9. 与 MLLM 不确定性量化的关系

这篇论文研究的是 **纯文本 LLM**，没有图像、音频或视频输入，因此不能直接当作 MLLM UQ 的实验证据。不过，它很适合形成多模态扩展：

1. **视觉语义梯度**：对视觉编码器中语义保持的 patch/token 表征求回答似然梯度；
2. **跨模态语义梯度**：对 projector 或 cross-attention 后的融合表征求梯度；
3. **模态分解**：分别计算文本梯度 $S_{text}$、视觉梯度 $S_{vision}$ 和融合梯度 $S_{fusion}$，定位不确定性来自问题理解、视觉感知还是跨模态对齐；
4. **多模态 SPS**：用同义问题、图像增强、语义等价图像和不匹配图文对，寻找对等价变换稳定、对语义改变敏感的内部表示；
5. **流形约束**：把梯度投影到由真实图像增强差分或图文释义差分张成的局部切空间，避免把不可实现的隐藏扰动误当作语义变化。

一个直接可验证的研究问题是：

> 在视觉问答中，错误来自“看错”时，视觉表征梯度是否显著高于文本表征梯度；错误来自语言先验时，这种模式是否反转？

如果成立，SemGrad 不仅能做 UQ，还可能成为 MLLM 错误来源归因工具。

## 10. 复现建议

按优先级可采用以下最小复现路线：

1. 先用 Llama 3.1 Instruct 8B + TruthfulQA 复现 SemGrad、ParaGrad、G-NLL；
2. 核对生成 prompt、greedy decoding、BEM 阈值和 AUROC 正负方向；
3. 复现 SPS 热力图，确认 `<|start_header_id|>` 与深层隐藏状态确实形成高 SPS 区域；
4. 对比“最高 SPS 单层”“深层一半拼接”“输入最后 token”三种表示；
5. 加入有限差分验证：沿释义差分方向扰动隐藏状态，检查梯度的一阶预测与实际概率变化是否一致；
6. 再迁移到 MLLM，分别对视觉 token、文本 token 与融合 token 计算梯度；
7. 除 AUROC/AURC 外，增加风险-覆盖曲线、跨数据集阈值迁移和经过校准后的 ECE/Brier score。

## 11. 最终评价

这是一篇思路简洁、实验现象明确的工作。它最重要的贡献不是“梯度越大越不确定”这一旧命题，而是指出自由生成中的参数梯度会被多答案分布干扰，并把求导对象改为经过 SPS 筛选的语义保持隐藏表征。TruthfulQA 上的优势和 SPS-AUROC 相关性为该方向提供了不错的初步证据。

但 SemGrad 目前应被理解为 **白盒、局部、未校准的语义敏感度分数**。语义流形是否被正确刻画、离线 SPS 的迁移成本、长文本与多模态可扩展性，以及跨任务概率校准，仍是决定它能否成为通用 UQ 方法的关键问题。

**个人评分：8/10。** 新颖性和研究启发性较强，实验覆盖与理论闭环尚可继续加强；对 MLLM UQ 项目尤其值得沿“分模态语义梯度 + 错误来源归因”方向跟进。
